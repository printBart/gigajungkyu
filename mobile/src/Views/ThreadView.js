import React from 'react';
import {
    StyleSheet,
    View,
    Text,
  } from 'react-native';

const ThreadView = () => {
  return (
    <View>
      <Text>Thread Views</Text>
    </View>
  );
}

const styles = StyleSheet.create({
});

export default ThreadView;